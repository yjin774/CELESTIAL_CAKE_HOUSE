package com.example.celestial.data

import android.content.Context
import android.graphics.Bitmap
//import com.google.mlkit.vision.barcode.BarcodeScanning
import com.google.mlkit.vision.common.InputImage
import kotlinx.coroutines.tasks.await

//object BarcodeUtils {
    /**
     * Scan the bitmap for barcodes and return the first code found, or null.
     */
//    suspend fun scanBarcodeFromBitmap(context: Context, bitmap: Bitmap): String? {
//        val image = InputImage.fromBitmap(bitmap, 0)
//        val scanner = BarcodeScanning.getClient()
//        val barcodes = scanner.process(image).await()
//        return barcodes.firstOrNull()?.rawValue
//    }
//}

